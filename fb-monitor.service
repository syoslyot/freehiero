# systemd service for freehiero
# Install: sudo cp fb-monitor.service /etc/systemd/system/
#          sudo systemctl enable --now fb-monitor

[Unit]
Description=FB Food Monitor
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/opt/freehiero
ExecStart=/opt/freehiero/.venv/bin/python scheduler.py
Restart=always
RestartSec=30
Environment=DISPLAY=:99

[Install]
WantedBy=multi-user.target
